from flask import Flask, request, jsonify
from flask.ext.cors import CORS
# http://stackoverflow.com/questions/372885/how-do-i-connect-to-a-mysql-database-in-python
# Windows: http://sourceforge.net/project/showfiles.php?group_id=22307
import MySQLdb

app = Flask(__name__)
cors = CORS(app)
db = MySQLdb.connect(host='localhost', user='root', password='', db='process_mining')


@app.route('/')
def hello():
    return 'Hello'


if __name__ == '__main__':
    app.run(debug=True)
