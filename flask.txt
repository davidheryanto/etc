from flask import Flask, request, jsonify
from flask.ext.cors import CORS

app = Flask(__name__)
cors = CORS(app)


@app.route('/')
def hello():
    return 'Hello'


if __name__ == '__main__':
    app.run(debug=True)
